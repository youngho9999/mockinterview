pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello, Jenkins! This is a simple test.'
                echo "Company Name: ${env.COMPANY_NAME}"
            }
        }
    }
}